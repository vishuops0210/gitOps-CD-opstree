# ============================================================
# Child Application — frontend2 (Dev)
# Purpose: Deploys frontend2 to the dev EKS cluster
# Sync: Auto
# ============================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-frontend2
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    environment: dev
    app: frontend2
spec:
  project: dev
  source:
    repoURL: https://github.com/YOUR_ORG/spring-boot-realworld-example-app.git
    targetRevision: HEAD
    path: helm/platform-app
    helm:
      valueFiles:
        - values.yaml
        - ../../applications/frontend2/dev-values.yaml
  destination:
    server: https://dev-eks-endpoint.eks.amazonaws.com
    namespace: frontend2
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
