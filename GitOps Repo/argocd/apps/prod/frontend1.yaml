# ============================================================
# Child Application — frontend1 (Prod)
# Purpose: Deploys frontend1 to the prod EKS cluster
# Sync: Manual + approval gate
# ============================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prod-frontend1
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    environment: prod
    app: frontend1
spec:
  project: prod
  source:
    repoURL: https://github.com/YOUR_ORG/spring-boot-realworld-example-app.git
    targetRevision: HEAD
    path: helm/platform-app
    helm:
      valueFiles:
        - values.yaml
        - ../../applications/frontend1/prod-values.yaml
  destination:
    server: https://prod-eks-endpoint.eks.amazonaws.com
    namespace: frontend1
  syncPolicy:
    # Manual sync only — auto-sync explicitly disabled for safety
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
