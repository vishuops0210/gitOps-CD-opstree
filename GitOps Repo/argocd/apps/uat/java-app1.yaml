# ============================================================
# Child Application — java-app1 (UAT)
# Purpose: Deploys app1 to the uat EKS cluster
# Sync: Manual (team lead triggers)
# ============================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: uat-java-app1
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    environment: uat
    app: java-app1
spec:
  project: uat
  source:
    repoURL: https://github.com/YOUR_ORG/spring-boot-realworld-example-app.git
    targetRevision: HEAD
    path: helm/platform-app
    helm:
      valueFiles:
        - values.yaml
        - ../../applications/app1/uat-values.yaml
  destination:
    server: https://uat-eks-endpoint.eks.amazonaws.com
    namespace: java-app1
  syncPolicy:
    # Manual sync only
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 2
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
