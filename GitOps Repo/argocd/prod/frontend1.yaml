# ============================================================
# PROD — frontend1
# Sync: Manual only (approval gate enforced by process)
# Approval: Dev Lead + Ops Lead must trigger explicitly
# ============================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend1-prod
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    environment: prod
    app: frontend1
spec:
  project: platform-apps
  source:
    repoURL: https://github.com/YOUR_ORG/spring-boot-realworld-example-app.git
    targetRevision: HEAD
    path: helm/platform-app
    helm:
      valueFiles:
        - values.yaml
        - ../../applications/frontend1/prod-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: frontend1-prod
  syncPolicy:
    # Manual sync only — auto-sync explicitly disabled for safety
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 1
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
