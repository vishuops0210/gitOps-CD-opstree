# ============================================================
# UAT — frontend1
# Sync: Manual only
# Approval: Team lead triggers via UI or CLI
# ============================================================
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: frontend1-uat
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    environment: uat
    app: frontend1
spec:
  project: platform-apps
  source:
    repoURL: https://github.com/YOUR_ORG/spring-boot-realworld-example-app.git
    targetRevision: HEAD
    path: helm/platform-app
    helm:
      valueFiles:
        - values.yaml
        - ../../applications/frontend1/uat-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: frontend1-uat
  syncPolicy:
    # Manual sync only — no automated section
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 2
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
