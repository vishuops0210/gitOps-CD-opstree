# ============================================================
# UAT values for app9
# Environment: User Acceptance Testing
# Sync: Manual (team lead triggers)
# ============================================================

nameOverride: "app9"
fullnameOverride: "app9"

replicaCount: 2

image:
  repository: "gcr.io/PROJECT_ID/app9"
  tag: "latest"

resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 250m
    memory: 512Mi

# UAT ingress
ingress:
  enabled: false
  className: "nginx"
  annotations: {}
  hosts:
    - host: app9-uat.example.com
      paths:
        - path: /
          pathType: Prefix

# Probes
livenessProbe:
  enabled: true
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  enabled: true
  httpGet:
    path: /health
    port: http
  initialDelaySeconds: 10
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 3

# UAT may test HPA but keep it off by default
autoscaling:
  enabled: false
  minReplicas: 2
  maxReplicas: 4

pdb:
  enabled: false

configmap:
  enabled: false
  data: {}

secret:
  enabled: false
  data: {}

networkPolicy:
  enabled: false
